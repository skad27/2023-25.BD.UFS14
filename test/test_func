
import pytest
import requests
from mymodule import get_pubchem_cid

def search_terms_in_pdf():
    session = requests.Session()
    ingredient_name = "Triacetin"
    cid = search_terms_in_pdf(session, ingredient_name)
    assert cid == "5541", "Il CID per il Triacetin dovrebbe essere '5541'"